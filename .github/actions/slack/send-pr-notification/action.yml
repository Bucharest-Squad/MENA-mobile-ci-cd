name: 'Send notification PR to Slack'

inputs:
  slack-bot-token:
    required: true
  slack-notification-channel-id:
    required: true
  bot-app-id:
    required: true
  branch-name:
    required: true
  message:
    required: true

runs:
  using: "composite"
  steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Install jq
      shell: bash
      run: |
        sudo apt-get update && sudo apt-get install -y jq

    - name: Send or retrieve region messages and send thread
      shell: bash
      env:
        SLACK_BOT_TOKEN: ${{ inputs.slack-bot-token }}
        SLACK_NOTIFICATION_CHANNEL_ID: ${{ inputs.slack-notification-channel-id }}
        BOT_APP_ID: ${{ inputs.bot-app-id }}
        BRANCH_NAME: ${{ inputs.branch-name }}
        MESSAGE: ${{ inputs.message }}
      run: |
        REGIONS=("IDN" "DKN" "CHT" "FTH" "TRN" "WLT")

        messages=$(curl -s -X POST "https://slack.com/api/conversations.history" \
          -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
          -H "Content-type: application/json" \
          --data "{\"channel\": \"$SLACK_NOTIFICATION_CHANNEL_ID\"}")

        bot_messages=$(echo "$messages" | jq --arg app_id "$BOT_APP_ID" '[.messages[] | select(.app_id == $app_id)]')
        message_count=$(echo "$bot_messages" | jq 'length')

        if [ "$message_count" -lt 6 ]; then
          echo "Sending default region messages to Slack..."
          for REGION in "${REGIONS[@]}"; do
            curl -s -X POST https://slack.com/api/chat.postMessage \
              -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
              -H "Content-type: application/json; charset=utf-8" \
              --data "{
                \"channel\": \"$SLACK_NOTIFICATION_CHANNEL_ID\",
                \"blocks\": [
                  {
                    \"type\": \"section\",
                    \"text\": {
                      \"type\": \"mrkdwn\",
                      \"text\": \"${REGION} prs info\"
                    }
                  }
                ]
              }"
          done

          sleep 2
          messages=$(curl -s -X POST "https://slack.com/api/conversations.history" \
            -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
            -H "Content-type: application/json" \
            --data "{\"channel\": \"$SLACK_NOTIFICATION_CHANNEL_ID\"}")

          bot_messages=$(echo "$messages" | jq --arg app_id "$BOT_APP_ID" '[.messages[] | select(.app_id == $app_id)]')
          message_count=$(echo "$bot_messages" | jq 'length')
        fi

        team=$(echo "$BRANCH_NAME" | cut -d'/' -f2)
        prefix="${team:0:3}"

        for i in "${!REGIONS[@]}"; do
          if [[ "${REGIONS[$i]}" == "$prefix" ]]; then
            thread_ts=$(echo "$bot_messages" | jq -r ".[$i].ts")

            echo "Replying to thread for region $prefix with timestamp $thread_ts..."

            curl -s -X POST "https://slack.com/api/chat.postMessage" \
              -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
              -H "Content-type: application/json; charset=utf-8" \
              --data "{
                \"channel\": \"$SLACK_NOTIFICATION_CHANNEL_ID\",
                \"thread_ts\": \"$thread_ts\",
                \"blocks\": [
                  {
                    \"type\": \"section\",
                    \"text\": {
                      \"type\": \"mrkdwn\",
                      \"text\": \"$MESSAGE\"
                    }
                  }
                ]
              }"
          fi
        done
